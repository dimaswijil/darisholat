# Scripts

| Script | Fungsi |
|---|---|
| `add_files_script.rb` | Mendaftarkan file Swift baru ke `project.pbxproj` (gem `xcodeproj`). Edit daftar file di dalamnya, **tutup Xcode dulu**, lalu jalankan `ruby scripts/add_files_script.rb` dari root repo. |
